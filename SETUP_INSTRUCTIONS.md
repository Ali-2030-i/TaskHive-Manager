# تعليمات إعداد قاعدة البيانات 🗄️

## المشكلة الحالية 🔴
صفحة البروفايل لا تعمل لأن جدول `user_profiles` غير موجود في قاعدة بيانات Supabase.

## الحل ✅

### الخطوة 1: دخول Supabase
1. اذهب إلى https://supabase.com/
2. سجل دخول إلى حسابك
3. اختر المشروع: **TaskHive – Team Task Manager**

### الخطوة 2: تنفيذ السكيما
1. اذهب إلى **SQL Editor** من القائمة اليسرى
2. انقر على **New Query**
3. انسخ محتوى الملف `supabase_schema.sql` بالكامل
4. الصق الكود في محرر SQL
5. انقر على **Run** أو اضغط `Cmd+Enter` (Mac) أو `Ctrl+Enter` (Windows)

⏳ سيستغرق بضع ثوان لتنفيذ الكود...

### الخطوة 3: التحقق من النجاح
1. اذهب إلى **Tables** من القائمة اليسرى
2. تحقق من وجود الجداول التالية:
   - ✅ `user_profiles`
   - ✅ `projects`
   - ✅ `tasks`
   - ✅ `sub_tasks`
   - ✅ `activities`

### الخطوة 4: إنشاء Storage Bucket (للصور)
1. اذهب إلى **Storage** من القائمة اليسرى
2. انقر على **Create a new bucket**
3. أدخل الاسم: `avatars`
4. اختر **Public** (لتكون الصور متاحة علنياً)
5. انقر على **Create bucket**

### الخطوة 5: إعادة تحميل التطبيق
1. عد إلى المتصفح
2. اضغط `F5` لإعادة تحميل الصفحة
3. جرب تسجيل الدخول والذهاب إلى صفحة البروفايل

## إذا لم تعمل بعد 🔧

### تحقق من:
1. **RLS Policies**: هل تم إنشاء السياسات بنجاح؟
2. **Trigger**: هل تم إنشاء trigger `on_auth_user_created`؟
3. **Console Errors**: افتح Developer Console (F12) وشاهد الأخطاء

### طلب المساعدة:
إذا ظهرت رسالة خطأ في Console، انسخها وأرسلها لكي أساعدك.

## الأوامر المفيدة في Supabase SQL

```sql
-- عرض جميع الجداول
SELECT table_name FROM information_schema.tables WHERE table_schema = 'public';

-- عرض الأعمدة في جدول معين
SELECT * FROM information_schema.columns WHERE table_name = 'user_profiles';

-- حذف جدول (احذر!)
DROP TABLE IF EXISTS public.user_profiles CASCADE;

-- عرض السياسات (RLS Policies)
SELECT * FROM pg_policies;

-- عرض ال Triggers
SELECT * FROM pg_trigger;
```

---

**نصيحة**: احفظ هذا الملف لأنك قد تحتاجه إذا أضفت جداول جديدة في المستقبل.
